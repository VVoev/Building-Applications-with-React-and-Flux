const React = require('react');
const toastr = require('toastr');
const { Navigation } = require('react-router');

const ManageFansPage = React.createClass({

    render() {
        return (<div>
            <h1>Manage Fans</h1>
        </div>
        );
    },
});

module.exports = ManageFansPage;